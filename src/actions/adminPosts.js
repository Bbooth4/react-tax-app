import * as types from './actionTypes';

export function admin() {
  return dispatch => {
    // database call here

    // return AuthorApi.getAdmin().then(admin => {
    //   dispatch(admin(admin));
    // }).catch(error => {
    //   throw(error);
    // });
  };
}
